/* A C library consumed from Tatamu via extern "C". */
#include <stdint.h>
#include <math.h>

int64_t c_mul(int64_t a, int64_t b) { return a * b; }

double c_hypot(double a, double b) { return sqrt(a * a + b * b); }
